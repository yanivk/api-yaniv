const config =
    {
        db: {
            host: 'localhost',
            user: 'root',
            password: '@Dmin770',
            database: 'express'
        },
        listPerPage: 10
    }
export default config
